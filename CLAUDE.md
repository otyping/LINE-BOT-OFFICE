# CLAUDE.md — บริบทโปรเจกต์สำหรับ Claude Code

อ่าน `README.md` ก่อน เพื่อดูที่มา ขั้นตอนการใช้งาน และวิธีตั้งค่า ไฟล์นี้สรุปเฉพาะเรื่องเทคนิคที่ต้องรู้ก่อนแก้โค้ด

ผู้ใช้สื่อสารเป็นภาษาไทย ข้อความทุกอย่างที่ผู้ใช้ปลายทางเห็น (UI, ข้อความบอท, error) ต้องเป็นภาษาไทย

## สถาปัตยกรรม

```
LINE group ──webhook──▶ Apps Script (Code.gs, web app /exec) ──▶ Google Sheets
     │                        ▲
     └─ uri ─▶ LIFF (index.html บน GitHub Pages) ──POST text/plain──┘
```

`doPost` แยกที่มาของคำขอจาก body

- body มี `events` แปลว่ามาจาก LINE webhook
- body มี `action` แปลว่ามาจาก LIFF API ได้แก่ `init`, `submit`, `pending`, `approve`, `reject`

## โครงสร้างข้อมูล 3 ชั้น

ห้ามกลับไปเขียนลงตารางแนวกว้างโดยตรง ระบบแยกที่เก็บข้อมูลออกจากหน้าตาราง

| ชีท | บทบาท | โตยังไง |
|---|---|---|
| `พนักงาน` | ทะเบียนคน **ไม่เคยลบใคร** คนลาออกใส่ `วันที่ออก` | เพิ่มแถวลงล่าง |
| `บันทึกเวร` | **แหล่งข้อมูลจริง** 1 แถว = 1 คน 1 วัน append-only | ต่อท้ายลงล่าง |
| `รอบ 2569-09` | ตารางแนวกว้างสำหรับดู/พิมพ์ สร้างจากบันทึกเวร | ลบทิ้งได้ กดสร้างใหม่ได้ |
| `คำขอ` | ประวัติคำขอทุกใบ (ผู้แจ้ง สรุป entries JSON สถานะ) | ต่อท้ายลงล่าง |
| `ผู้ใช้ระบบ` | userId, ชื่อ, บทบาท (`หัวหน้างาน`/`HR`), แผนกที่ดูแล | แก้มือ |

### กติกาของ `บันทึกเวร`

- คอลัมน์: `วันที่ | รหัส | ชื่อ | กะ | สถานะ | เลขคำขอ | ผู้แจ้ง | ผู้พิจารณา | เวลาอัปเดต`
- สถานะมี 4 ค่า: `รออนุมัติ`, `อนุมัติ`, `ไม่อนุมัติ`, `แทนที่`
- **ค่าจริงของ (วัน, คน) = แถวสถานะ `อนุมัติ` ที่อยู่ล่างสุด** ดู `dayState_()`
- อนุมัติ = เปลี่ยนแถว `รออนุมัติ` ของคำขอนั้นเป็น `อนุมัติ` แล้วมาร์คแถว `อนุมัติ` เดิมของคน-วันเดียวกันเป็น `แทนที่`
- ไม่อนุมัติ = เปลี่ยนแถวเป็น `ไม่อนุมัติ` เฉย ๆ **ไม่ต้องคืนค่าเดิม** เพราะแถวที่อนุมัติไว้ไม่เคยถูกแก้
- ห้ามลบแถว เพราะจะทำให้ประวัติและการอ้างอิง `เลขคำขอ` เสีย

### รอบสรุป

- รอบ = วันที่ 26 เดือนก่อน ถึง 25 เดือนนี้ (`CFG.CUT_DAY`) `period_(ymd)` คืน `{key, start, end, label}`
- `key` เป็น พ.ศ. เช่น `2569-09` ชื่อชีทคือ `CFG.VIEW_PREFIX + key`
- ฟอร์มให้เลือกวันได้ตั้งแต่ **ย้อนหลัง `BACK_DAYS` (7) วัน** ถึง **สิ้นรอบถัดไป** (`formRange_()`) วันที่ไม่ได้อ่านจากชีทอีกแล้ว จึงไม่มีปัญหา "หยุดที่ช่องว่างแรก" แบบเวอร์ชันเดิม
- `buildPeriodSheet_()` ล้างชีทรอบนั้นแล้วสร้างใหม่ทั้งใบทุกครั้ง ปลอดภัยเพราะไม่ใช่แหล่งข้อมูลจริง

## ข้อจำกัดสำคัญ (อย่าเปลี่ยนโดยไม่เข้าใจ)

- **Apps Script อ่าน HTTP header ไม่ได้** จึงตรวจ `x-line-signature` ไม่ได้ ด้วยเหตุนี้ webhook ทำได้แค่ **reply** เท่านั้น ส่วนการกระทำที่เปลี่ยนข้อมูล (submit, approve, reject) ต้องผ่าน LIFF ซึ่งส่ง `idToken` มาให้ `verifyIdToken_()` ตรวจกับ LINE และอ่านสิทธิ์จากชีท `ผู้ใช้ระบบ` ห้ามเพิ่มการอนุมัติผ่าน postback ใน webhook
- **LIFF เรียก Apps Script ต้องใช้ `Content-Type: text/plain`** เพื่อเลี่ยง CORS preflight
- **โควตา push ของ LINE OA แพ็กเกจฟรี** ใช้ push เฉพาะแชทส่วนตัว (แจ้ง HR และแจ้งผลกลับผู้ส่ง) ห้าม push เข้ากลุ่ม เพราะนับตามจำนวนสมาชิก ถ้าต้องสื่อสารในกลุ่ม ให้ใช้ reply หรือ `liff.sendMessages` (ส่งในนามผู้ใช้)
- **LINE Login channel ต้องอยู่ Provider เดียวกับ Messaging API** ไม่อย่างนั้น userId จะไม่ตรงกัน
- **แก้ `Code.gs` แล้วต้อง Deploy → New version** ไม่อย่างนั้น `/exec` ยังรันโค้ดเก่า

## รายละเอียดที่ต้องระวัง

- **การเรียกบอทในกลุ่ม:** ใช้ชื่อบอทเป็นคำนำ (`CFG.BOT_NAME` = `จำปี`, คำอื่นใน `CFG.BOT_ALIASES`) ดู `botCommand_()` ซึ่งคืนคำสั่งที่เหลือหลังตัดชื่อ หรือ `null` ถ้าไม่ได้เรียกบอท ในกลุ่มถ้า `null` ต้องเงียบ ส่วนแชทส่วนตัวตอบเสมอ ทุกคำตอบแนบ `quickReply_()` เพื่อให้กดต่อได้โดยไม่ต้องพิมพ์
- **รูปแบบเวลา:** ชั่วโมงเริ่มไม่เติม 0 ส่วนชั่วโมงเลิกเติม 0 ให้ครบ 2 หลัก เช่น `8.00-17.00`, `21.00-05.00` (ดู `normShift_` ซึ่งต้องตรงกันทั้งใน Code.gs และ index.html)
- **วันที่ทุกที่ในโค้ดเป็นสตริง `YYYY-MM-DD` ค.ศ.** เทียบกันด้วย `<` `>` ตรง ๆ ได้ พ.ศ. ใช้แค่ตอนแสดงผล (`thDate_`, `period_().key`)
- คอลัมน์ `วันเริ่มงาน`/`วันที่ออก` ในชีท `พนักงาน` ต้องเป็นวันที่จริงหรือสตริง `YYYY-MM-DD` ถ้ากรอกเป็นข้อความรูปแบบอื่น `checkSetup()` จะเตือน
- ฟอร์มแสดงเฉพาะคนที่ **ยังทำงานอยู่ตั้งแต่วันนี้เป็นต้นไป** ส่วน `apiSubmit_` ตรวจซ้ำด้วย `activeOn_(e, วันที่ที่แจ้ง)`
- **ห้าม merge เซลล์ในชีทรอบ** เพราะ `buildPeriodSheet_()` ตรึง 5 คอลัมน์แรก ถ้ามี merge คร่อมเส้นตรึง `setFrozenColumns` จะ error ตอนสร้างชีท หัวเรื่องแถว 1 จึงปล่อยข้อความล้นแทนการ merge และตอนรีเฟรชต้อง `breakApart()` ล้าง merge เก่าก่อนเสมอ (mock ในเทสต์จำลองกติกานี้ไว้แล้ว)
- **สีในชีทรอบเป็นการจัดรูปแบบตามเงื่อนไข** (`(รอ)` = ส้ม, `หยุด/ลากิจ/ลาป่วย/ขาดงาน` = เหลือง, หัวคอลัมน์วันอาทิตย์ = เหลือง) อย่ากลับไปใช้สีที่ระบายมือ เพราะสร้างชีทใหม่แล้วจะหาย
- เลขคำขอใช้รูปแบบ `R` + เลข 4 หลัก จากจำนวนแถวในชีท `คำขอ` (สร้างภายใต้ `LockService`)
- `migrateFromLegacy()` อ่านชีท `รหัสพนักงาน` แบบกว้าง เก็บ**ทุกช่องในแถว 2 ที่เป็นวันที่** จึงข้ามช่องว่างระหว่างบล็อกได้ และรันซ้ำได้โดยไม่เพิ่มข้อมูลซ้ำ (เช็คจาก `วัน|รหัส` ที่มีอยู่แล้ว)

## การทดสอบ

- `node tests/mock-test.js` จำลอง SpreadsheetApp, UrlFetchApp และ LINE แล้วรัน migrate → init → submit → approve (แก้เวลา) → แทนที่เวรเดิม → reject → ตรวจสิทธิ์ → พนักงานลาออก → สร้างชีทรอบ ใช้ทดสอบหลังแก้ `Code.gs` ทุกครั้ง (exit code ไม่เป็น 0 ถ้ามีข้อตก)
- วันที่ในเทสต์อิงวันปัจจุบันเสมอ (`D(n)`) จึงไม่พังเมื่อเวลาผ่านไป
- `index.html` ทดสอบในเบราว์เซอร์ได้ด้วยการ mock `window.liff` และ `fetch` เช่นใช้ Playwright
- ใน Apps Script จริง ใช้เมนู **ตารางทำงาน → ตรวจการตั้งค่า** (`checkSetup()`) ตรวจการอ่านชีทและ Script properties

## งานที่อาจทำต่อ

- แจ้งเตือน HR เป็นสรุปวันละครั้ง แทนการส่งทุกคำขอ เพื่อประหยัดโควตา
- เก็บ OT เป็นคอลัมน์ในบันทึกเวร แล้วสรุปชั่วโมง OT รายคนต่อรอบ
- สร้างชีทรายคน (แบบแท็บ `KB036`, `013` ในไฟล์เดิม) จากบันทึกเวรด้วยสคริปต์
- ตั้ง trigger รายวันให้รีเฟรชชีทรอบปัจจุบันอัตโนมัติ
